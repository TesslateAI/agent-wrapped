{
    "apps": {
        "agent-wrapped": {
            "directory": ".",
            "port": 3000,
            "start": "npm install && npm run dev -- --hostname 0.0.0.0",
            "build": "npm run build",
            "output": "out",
            "framework": "nextjs",
            "env": {}
        }
    },
    "infrastructure": {},
    "primaryApp": "agent-wrapped"
}
